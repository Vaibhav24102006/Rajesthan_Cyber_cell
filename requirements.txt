# Official Paddle wheels (CPU). Required for reliable paddlepaddle install on Windows.
--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cpu/

# Web framework & backend engine
fastapi
uvicorn[standard]
sqlalchemy
pydantic
python-dotenv
requests
python-multipart
psutil

# Pin Paddle for Python 3.11 x64 Windows CPU (Ultra-stable production stack)
paddlepaddle==2.6.2
paddleocr==2.7.0.3

# Core mathematical & image processing dependencies
# Pin numpy to < 2.0.0 to prevent binary breaking changes with Paddle C extensions
numpy>=1.20.0,<2.0.0
# Pin opencv-python range to avoid experimental or incompatible builds
opencv-python>=4.6.0,<4.11.0
pillow>=9.0.0

# Supporting OCR stack packages
scikit-image>=0.19.0,<0.25.0
shapely>=2.0.0,<2.2.0
imgaug>=0.4.0
visualdl>=2.2.0
pdf2docx>=0.5.6
rapidfuzz>=3.0.0
pyclipper>=1.3.0

# Document parsers & low-confidence fallback engine
pymupdf>=1.19.0
pytesseract>=0.3.10

# Quality assessment & evaluation (CER/WER)
jiwer>=3.0.0
